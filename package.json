{
  "name": "lambda-dynamodb-crud",
  "version": "1.0.0",
  "description": "Lambda function for DynamoDB CRUD operations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
